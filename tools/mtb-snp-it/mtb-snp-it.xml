<tool id="mtb-snp-it" name="SNP-IT" version="@TOOL_VERSION@+galaxy0">
    <description>Mycobacterial lineage typing.</description>
    <macros>
        <token name="@TOOL_VERSION@">1.1</token>
    </macros>
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">mtb-snp-it</requirement>
    </requirements>
    <command detect_errors="exit_code">
        <![CDATA[
        ln -s '${input}' '$input.element_identifier' &&
        snpit-run.py --input '$input.element_identifier' | tee $output
        ]]>
    </command>
    <inputs>
        <param type="data" argument="--input" format="vcf,vcf.gz,fasta,fasta.gz" label="Input VCF or FASTA" />
    </inputs>
    <outputs>
        <data name="output" format="tabular" label="${input.element_identifier} lineage" />
    </outputs>
    <tests>
        <test>
            <param name="input" value="example.vcf" ftype='vcf' />
            <output name="output" file="example.tab" ftype="tabular" />
        </test>
    </tests>
    <help>
        <![CDATA[
        SNP-IT processes variants in M. tuberculosis identified relative to the H37Rv reference genome
        and uses these to identify the lineage of the associated sample. Input is either a VCF or FASTA file
        containing variants. In the case of a FASTA file it needs to be the same length (i.e. coordinates) as
        the H37Rv reference genome.
        ]]>
    </help>
    <citations>
        <citation type="doi">10.3201/eid2503.180894</citation>
    </citations>
</tool>
